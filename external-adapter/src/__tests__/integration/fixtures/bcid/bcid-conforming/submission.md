# Multi-model consensus

## Why several models beat one

Independently trained models fail in different ways, so agreement between them
is stronger evidence than any single verdict. A submission crafted to exploit
one model's preferences rarely exploits another's at the same time.
